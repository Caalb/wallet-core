includes:
    - phpstan-baseline.neon

parameters:
    level: 9
    paths:
        - app
    excludePaths:
        - app/Kernel.php

    bootstrapFiles:
        - vendor/autoload.php

    ignoreErrors: []

    checkMissingIterableValueType: true

    tmpDir: runtime/phpstan
